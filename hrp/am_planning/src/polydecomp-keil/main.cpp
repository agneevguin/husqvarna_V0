#include <vector>
#include <stdlib.h>
#include <stdio.h>
#include <limits>
#include "polygon.h"

int winWidth = 800, winHeight = 600;
int mouse_x, mouse_y;
Polygon poly;
EdgeList diags;
bool done = false;


int main(int argc, char** argv) {
    char buf[80];

    while (1) {
      
    }
    return EXIT_SUCCESS;
}

